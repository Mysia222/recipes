// import React from 'react';
// import hashtagStore from '../stores/tagsStore'

// const HashtagContext = React.createContext(null)

// export const HashtagProvider = ({children}) => {
//   const notesStore = hashtagStore;

//   return (<HashtagContext.Provider value={hashtagStore}>
//     {children}
//   </HashtagContext.Provider>
//   )
// }

// export const useNotesStore = () => React.useContext(NotesContext)
export {};